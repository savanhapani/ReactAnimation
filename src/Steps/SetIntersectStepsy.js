import React from "react"
import { exact } from "prop-types";

function Steps()
{
    return(

        <div>
        <div id="myDIV" class="column" className="textarea1">
            <span style={{marginLeft:"20px"}}>(1)  The set A contains some elements and set B contain some elements.</span><br /><br />
            <span style={{marginLeft:"20px"}}>(2)  When the function is executed, it will find set intersection A⋂B.</span><br /><br />
            <span style={{marginLeft:"20px"}}>(3)  All the elements that are in set A as well as in set B will be returned.</span><br /><br />
    </div>
    </div>
    )
}

export default Steps